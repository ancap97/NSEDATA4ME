# Generated data is kept off main: `python publish_data.py` puts it on the `data` branch
# instead (it force-adds these paths). Only hand-edited inputs are tracked here.
/data/*
!/data/actions/
/data/actions/*
!/data/actions/manual_overrides.csv

# Python
.venv/
__pycache__/
*.pyc

# Claude Code project notes (local only)
CLAUDE.md
